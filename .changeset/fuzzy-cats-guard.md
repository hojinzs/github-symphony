---
"@gh-symphony/cli": patch
---

Keep `USERPROFILE` aligned with the isolated agent child home in both built-in runtimes while preserving declared secret exclusions from the child environment (#870).
