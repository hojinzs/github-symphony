---
"@gh-symphony/cli": patch
---

Report stale standalone project workflow copies in status and doctor while treating repositories with no committed `WORKFLOW.md` as an intentional non-warning policy layout (#952).
