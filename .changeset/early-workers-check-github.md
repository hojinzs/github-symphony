---
"@gh-symphony/cli": patch
---

Fail GitHub and Linear workers before agent launch when their effective tracker credential is missing, expose skipped dispatches and remediation in status warnings, and make `doctor` validate the selected tenant's worker GitHub credential for #813.
