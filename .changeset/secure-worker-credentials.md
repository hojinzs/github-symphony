---
"@gh-symphony/cli": patch
---

Inject tenant-scoped tracker credentials into worker host operations while preserving agent-child secret isolation, including broker URLs and token-cache paths, and document atomic Linear credential scope precedence (#809).
