---
"@gh-symphony/cli": patch
---

Stage host Docker CLI plugins into isolated Codex and Claude child homes without exposing Docker registry credentials, so worker agents can resolve user-level Compose installations (#825).
