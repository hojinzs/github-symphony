---
"@gh-symphony/cli": patch
---

Reject malformed or unknown `SYMPHONY_WORKFLOW_HOOK_ENV_ALLOWLIST` entries instead of silently ignoring them, and document the coding-agent child environment boundary (#871).
