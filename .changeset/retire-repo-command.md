---
"@gh-symphony/cli": major
---

Retire the `repo` command surface in favor of explicit `project start --project-dir <path>` orchestration, including safe daemon respawn and legacy daemon stopping (#849).
