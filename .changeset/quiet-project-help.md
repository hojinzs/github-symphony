---
"@gh-symphony/cli": patch
---

Expose standalone `project` commands and supported `project start` runtime flags in CLI help, reject misspelled runtime options, and clarify the `--assigned-only` migration guidance for #625.
