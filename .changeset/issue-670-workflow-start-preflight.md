---
"@gh-symphony/cli": patch
---

Validate configured repository and project workflows before startup, preserve reconciliation when a tracker adapter becomes unsupported, and honor `repo init --workflow-file` at runtime (#670).
