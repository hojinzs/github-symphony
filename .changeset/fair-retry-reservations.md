---
"@gh-symphony/cli": patch
---

Fairly prioritize overdue retry reservations during orchestrator capacity contention so each retry gets a turn without consuming its failure budget or retry backoff. (#703)
