---
"@gh-symphony/cli": patch
---

Reject host Git and SSH credential environment names in custom runtime authentication settings instead of silently removing them from the agent child environment (#868).
