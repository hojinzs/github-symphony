---
"@gh-symphony/cli": patch
---

Resolve each worker's project environment after workspace hooks so same-run `.env` refreshes, credential checks, and the recorded environment digest stay consistent (#834).
