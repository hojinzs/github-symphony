---
"@gh-symphony/cli": patch
---

Remove the obsolete repository-root environment doctor check now that projects are folder-addressed, and document the `project start --project-dir <path>` migration for #851.
