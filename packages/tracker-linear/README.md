# @gh-symphony/tracker-linear

Linear polling and normalized tracker integration behind the core tracker
adapter contract.

The canonical compact adapter profile and `linear_graphql` tool contract are
in [docs/trackers/linear.md](../../docs/trackers/linear.md).
